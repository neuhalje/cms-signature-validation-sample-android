CMS Signature Validation (android)
=====================================

This is a very simple PoC on how to validate a detached CMS signature with bouncy castle *on android*.

The scaffolding code is the android default demo app hacked together.